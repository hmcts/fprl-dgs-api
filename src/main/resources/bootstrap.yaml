spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/div
      aliases:
        fprl.microservicekey-fprl-dgs-api: AUTH_PROVIDER_SERVICE_CLIENT_KEY
        fprl.docmosis-api-key: DOCMOSIS_SERVICE_ACCESS_KEY
        fprl.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
